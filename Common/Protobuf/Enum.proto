// .proto 버전
syntax = "proto3";

// package name
package Protocol;

enum PlayerType
{
	// 반드시 0 값이 있어야 함
	PLAYER_TYPE_NONE = 0;
	PLAYER_TYPE_KNIGHT = 1;
	PLAYER_TYPE_MAGE = 2;
	PLAYER_TYPE_ARCHER = 3;
	PLAYER_TYPE_WARRIOR = 4;
}